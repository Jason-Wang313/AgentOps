const config = {
  plugins: {
    // The new Tailwind v4 plugin
    '@tailwindcss/postcss': {},
    // Now installed, this will no longer crash the build
    autoprefixer: {},
  },
};

export default config;